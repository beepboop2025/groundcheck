# Root requirements — used by Vercel's Python runtime to build api/index.py.
# (Local dev uses engine/requirements.txt, which also includes uvicorn + pytest.)
fastapi>=0.110
httpx>=0.27
pydantic>=2.6
# Ed25519 response attestation (engine/groundcheck_engine/attest.py)
cryptography>=42

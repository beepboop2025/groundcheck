fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
pytest>=8

# Ed25519 response attestation (groundcheck_engine/attest.py)
cryptography>=42

# free-llm-router is the canonical Python twin and is NOT on PyPI. For local dev the
# engine imports it from the sibling checkout via GROUNDCHECK_ROUTER_PATH
# (default /Users/mrinal/free-llm-router/python). For Docker, vendor it first:
#   make vendor-router

# Official x402 SDK: source of truth for v2 wire shapes (402 envelope models)
x402>=2.15,<3
